[package]
name = "genai-client"
version = "0.1.0"
edition = "2024"

[dependencies]
# rust-genai = { path = ".." } # REMOVED CYCLIC DEPENDENCY
reqwest = { version = "0.12.15", features = ["json", "rustls-tls", "stream"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
tokio = { version = "1.44.2", features = ["full"] }
futures-util = "0.3"
async-stream = "0.3"
thiserror = "1.0"
bytes = "1.0"

# [[bin]] # REMOVED BINARY FROM INTERNAL CLIENT
# name = "simple_request"
# path = "src/bin/simple_request.rs"
